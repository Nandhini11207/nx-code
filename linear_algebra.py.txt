import numpy
n = int(input())
matrix = []
for _ in range(n):
    row = list(map(float, input().split()))
    matrix.append(row)
determinant = numpy.linalg.det(matrix)
print(round(determinant, 2))

